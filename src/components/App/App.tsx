import React, { FC } from 'react';
import { AppWrapper } from './App.styled';

interface AppProps {}

const App: FC<AppProps> = () => (
 <AppWrapper>
    App Component
 </AppWrapper>
);

export default App;
